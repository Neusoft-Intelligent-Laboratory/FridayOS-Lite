# 🛠️ FridayOS‑Lite 安装指南（小白版）

> 跟着点就行。**全程约 20 分钟，不用会写代码，不用翻墙。**
> 装之前强烈建议先花 3 分钟看 👉 [`工具说明.md`](./工具说明.md)，明白每个工具是干嘛的，装起来才不迷糊。
>
> 🌐 English: [`INSTALL.en.md`](./INSTALL.en.md) · 卡住了看 👉 [`常见问题.md`](./常见问题.md)

![FridayOS-Lite 系统结构图 —— 装好后五个工具怎么协作](./docs/images/architecture.png)

> 上面这张是总览：装完之后，你 → Obsidian(里的 Claudian) → Claude Code 引擎 → cc-switch → DeepSeek，答案再回到你。下面就按这个顺序一步步装。

---

## 📋 开始前：你需要准备

| 项目 | 说明 |
|---|---|
| 💻 一台电脑 | Windows 或 Mac 都行（Claudian 暂不支持手机/平板） |
| 🌐 能上网 | **不需要翻墙**，普通网络即可 |
| ⏱️ 约 20 分钟 | 第一次装，慢慢来 |
| 💰 约 ¥10 | 给 DeepSeek 充点钱（用量极省，¥10 能体验很久） |

---

## ⚠️ 先说一句：安装顺序 ≠ 讲解顺序

[`工具说明.md`](./工具说明.md) 里是按"由表及里"讲的：**Obsidian → Claudian → Claude Code → cc-switch → DeepSeek**，方便你理解。

但**动手装**的时候顺序要调一下，原因很简单：
- **cc-switch 需要先有 DeepSeek 的钥匙**才能配置 → 所以"注册 DeepSeek 拿钥匙"要提前。
- **Claudian 是最后请进门的客人**：得先把引擎（Claude Code）和芯片（DeepSeek）都接好，它连上来才有东西用。

所以实际动手顺序是下面这样 👇，照着做就对了：

```
第0步 Node.js(地基) → 第1步 Obsidian+模板(身体) → 第2步 DeepSeek钥匙(拿芯片)
   → 第3步 Claude Code(装引擎) → 第4步 cc-switch(接芯片) → 第5步 Claudian(请AI进门) → ✅完工
```

---

## 第 0 步：装 Node.js（地基，5 分钟）

**为什么**：Claude Code 和 cc-switch 都是建在 Node.js 上的，它是"地基"。装一次，以后不用管。

1. 打开官网 👉 **https://nodejs.org**
2. 点页面上那个 **"LTS"**（长期支持版）的大按钮，下载安装包。
3. 双击安装，**一路点"下一步 / Next"** 到底，全部用默认即可。
4. 验证：
   - Windows：按 `Win + R`，输入 `cmd`，回车，打开黑色窗口。
   - Mac：打开"终端 / Terminal"。
   - 输入这行再回车：
     ```
     node -v
     ```
   - 屏幕上**冒出一串版本号**（比如 `v20.11.0`）就成功了 ✅。看不懂没关系，有数字就行。

> 📸 *（截图位置：Node.js 官网 LTS 下载按钮）*

---

## 第 1 步：装 Obsidian + 打开大脑模板（5 分钟）

**为什么**：Obsidian 是你第二大脑的"身体"，先把它和 FridayOS‑Lite 模板准备好。

1. 打开官网 👉 **https://obsidian.md** → 点 **"Download"** → 选你的系统下载 → 双击安装。
2. 找到 FridayOS‑Lite 给你的模板文件夹 **`vault-template`**（如果是压缩包 `vault-template.zip`，先**右键解压**）。
   - 建议把它复制到一个好找的位置，并改名，比如 `我的大脑`，放在"文档"里。
3. 打开 Obsidian，第一个界面点 **"Open folder as vault"（打开文件夹作为仓库）**。
4. 选中你刚才那个 **`我的大脑`** 文件夹 → 打开。
5. 如果弹出"是否信任作者"，点 **"Trust author and enable plugins"（信任并启用）**。
6. 成功！左边能看到 `inbox / exec / wiki / skills / raw / system` 几个文件夹，
   先打开 **`先看我-START-HERE`** 那篇读一下。

> 此刻你的大脑有了"身体"，但还是"植物人"——别急，下面给它接 AI。
>
> 📸 *（截图位置：Obsidian "Open folder as vault" 界面 + 打开后的左侧文件夹树）*

---

## 第 2 步：注册 DeepSeek + 充值 + 拿 API Key（5 分钟）

**为什么现在做**：下一步装好引擎后，要立刻用这把"钥匙"。先拿到手，后面不用来回切。

1. 打开 👉 **https://platform.deepseek.com**
2. 用手机号 / 邮箱**注册并登录**。
3. **充值**：找到"充值 / 余额"，充 **¥10** 就够体验很久（按用量扣费，省得很）。
4. **创建钥匙**：左侧点 **"API keys"** → **"创建 / Create new API key"** → 给它起个名字（随便，比如 `friday`）。
5. ⚠️ **极其重要**：弹出的那串 `sk-xxxxxxxx...` **只显示这一次！**
   立刻**复制**，粘贴到记事本里**临时存好**（下一步要用）。
   万一没存到——别慌，删掉重新建一把就行。

> 🔒 这把钥匙 = 你账户的密码，**别发到群里、别上传网上**。
>
> 📸 *（截图位置：DeepSeek 创建 API Key 弹窗 + 充值页面）*

**顺便记住这两个值，第 4 步要填：**
- 接口地址（Anthropic 兼容）：`https://api.deepseek.com/anthropic`
- 模型名：`deepseek-v4-pro[1m]`（主力，聪明）和 `deepseek-v4-flash`（轻快，省钱）

---

## 第 3 步：装 Claude Code 引擎（一次性，免登录，免翻墙，5 分钟）

**为什么必须装**：很多人以为"装了 Claudian 就够了"，其实不是——**Claudian 只是个外壳**，
它自己不会思考，背后是把 Claude Code 当引擎在调用（这是它官方写明的硬性要求）。
没有这个引擎，Claudian 会报 `spawn claude ENOENT` 之类的错，根本动不了。

> ✅ **放心**：这一步**只是装个引擎程序，不用订阅、不用登录 Anthropic 账号、不用翻墙**。
> 那些要花钱、要翻墙的部分，下一步用 cc-switch + DeepSeek 全替你免掉了。
> 一句话：**Claude Code = 引擎（装一次就好）；cc-switch = 给引擎换上 DeepSeek 便宜芯片。**

1. 打开命令行窗口（Windows：`Win+R` → `cmd` → 回车；Mac：打开"终端"）。
2. **复制下面这一整行**粘进去，回车（用的是国内镜像，**不用翻墙**）：
   ```
   npm install -g @anthropic-ai/claude-code --registry=https://registry.npmmirror.com
   ```
3. 等它跑完（可能要一两分钟，刷一堆字是正常的）。
4. 验证：输入
   ```
   claude --version
   ```
   **冒出版本号**就成功了 ✅。

> 💡 装的时候先别急着运行 `claude` 去登录——它默认连的是要花钱、要翻墙的官方模型。
> 我们下一步用 cc-switch 把它改连 DeepSeek，之后就不用登录、不用翻墙了。
>
> 📸 *（截图位置：cmd 里 `claude --version` 显示版本号）*

---

## 第 4 步：装 cc-switch + 接上 DeepSeek（3 分钟）⭐ 关键一步

**为什么**：cc-switch 是"切换器"，点两下就让上一步的引擎改用 DeepSeek 芯片，不用碰任何代码。

1. 打开官网 👉 **https://ccswitch.io**（或 GitHub 下载页 **https://github.com/farion1231/cc-switch/releases**）。
2. 下载对应你系统的安装包：
   - Windows 选 `.msi` 或 `.exe`；Mac 选 `.dmg`。
   - 下载后双击安装，一路默认。
3. 打开 cc-switch，顶部默认就是 **Claude** 这一栏（我们就用这个）。
4. 点右上角 **"+"** 新建一个 Provider（服务商）：
   - 在预置列表里找到并选择 **DeepSeek**。
   - **API Key**：粘贴你第 2 步存的那串 `sk-...`。
   - **接口地址 / Base URL**：确认是 `https://api.deepseek.com/anthropic`。
   - **模型名**：填上 `deepseek-v4-pro[1m]` 和 `deepseek-v4-flash`
     （`[1m]` 表示开启超长上下文，建议留着）。
   - 保存。
5. 进 cc-switch 的 **设置 / Settings**，打开这两个开关（让一切自动化、少弹窗）：
   - ✅ **Apply to Claude Code Plugin**（让 cc-switch 接管模型切换）
   - ✅ **Skip Claude Code Initial Setup**（跳过首次啰嗦的设置）
6. 回到主界面，**选中刚建的 DeepSeek**，点 **"同步 / Sync to All Apps"**。

> 🎉 现在 Claude Code 引擎已经在用 DeepSeek 了！想验证：命令行里 `cd` 到你的大脑文件夹，
> 输入 `claude`，第一次问"是否信任"选第一项 **I trust**，然后随便问一句，能回话就说明引擎+芯片都通了。
> （以后想换 Kimi、智谱 GLM，也是在 cc-switch 里点一下就切。）
>
> 📸 *（截图位置：cc-switch 新建 DeepSeek provider 填写界面 + Settings 两个开关）*

---

## 第 5 步：装 Claudian，把 Friday 请进 Obsidian（3 分钟）

**为什么放最后**：引擎和芯片都接好了，现在让 AI"住进"你的笔记软件，从此告别命令行。

1. 回到 Obsidian → 左下角 **设置（齿轮图标）**。
2. 左侧点 **"Community plugins（第三方插件）"** → 第一次会让你点 **"Turn on community plugins（开启）"**。
3. 点 **"Browse（浏览）"** → 搜索框输入 **`Claudian`** → 找到它点 **"Install（安装）"** → 再点 **"Enable（启用）"**。
4. 进 Claudian 的设置，**Provider（服务商）选 "Claude"（即 Claude Code）**。
   - 它会自动用你电脑上那台、已经被 cc-switch 接到 DeepSeek 的 Claude Code 引擎。
5. 关掉设置。Obsidian 左侧或右侧应该出现一个 **Claudian 图标**，点开就是和 Friday 的聊天框。

> ⚠️ 要求 Obsidian 版本 ≥ 1.8.9（新装的肯定满足）。仅支持电脑端。
>
> 📸 *（截图位置：Obsidian 社区插件搜索 Claudian + Claudian 侧边栏聊天框）*

---

## ✅ 完工！试一句话验证

在 Claudian 的聊天框里，对 Friday 说：

> **你好 Friday，帮我记到 inbox：明天下午三点和张总开会。**

如果它**自己在 `inbox/` 文件夹里建了一条笔记**——

🎉 **恭喜，你的 AI 第二大脑 Friday 正式上线了！**

接下来可以试试：
- "帮我把这周要做的三件事放进 exec。"
- "读一下 raw 里的这份文档，提炼要点存到 wiki。"
- "我最近在跟进 XX 项目，帮我建个 wiki 页面。"

---

## 🆘 卡住了？

| 现象 | 怎么办 |
|---|---|
| `node -v` / `claude --version` 没反应 | 关掉命�